{
  "name": "bunnyland-3d-web",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "assets": "node scripts/generate-3d-assets.mjs",
    "build": "tsc -p tsconfig.json && vite build",
    "test": "node --test",
    "check": "npm run build && npm test",
    "playwright": "scripts/playwright-world-3d",
    "itch-screenshots": "scripts/playwright-itch-screenshots"
  },
  "dependencies": {
    "@bunnyland/ui-web": "file:vendor/bunnyland-ui-web-0.2.0.tgz",
    "preact": "^10.29.0",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.0",
    "@types/three": "^0.169.0",
    "typescript": "^5.9.0",
    "vite": "^8.1.0"
  }
}
